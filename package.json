{
  "name": "lena-react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-mpa.js && vite",
    "build": "node scripts/generate-mpa.js && vite build",
    "build:gh": "cross-env GITHUB_PAGES=true node scripts/generate-mpa.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "cross-env": "^7.0.3",
    "vite": "^5.4.2"
  }
}

